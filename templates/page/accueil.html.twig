{% extends 'base.html.twig' %}

{% block title %}
	Accueil
{% endblock %}

{% block body %}

	<div class="bg-white">
		<div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8">
			<h2 class="text-2xl font-bold tracking-tight text-gray-900">Nos produits populaires !</h2>


			{# boucle for produit in produits #}
			<section id="productList" class="w-fit mx-auto grid grid-cols-1 lg:grid-cols-3 md:grid-cols-2 justify-items-center justify-center gap-y-20 gap-x-10 mt-10 mb-5">
				{% for product in produits [:3]%}
					<div class="w-72 bg-white shadow-md rounded-xl duration-500 hover:scale-105 hover:shadow-xl">
						<a href="{{ path('product_detail', {'slug': product.slug}) }}">
							<img src="{{ asset('images/products/' ~ product.imageName)}}" alt="Product" class="h-80 w-72 object-cover rounded-t-xl"/>
							<div class="px-4 py-3 w-72">
								<span class="text-gray-400 mr-3 uppercase text-xs">{{product.category}}</span>
								<p class="text-lg font-semibold text-black truncate block capitalize">{{product.name}}</p>
								<div class="flex items-center">
									{# <p class="text-m font-semibold text-black cursor-auto my-3">{{product.price|number_format(2, ',')}}€</p> #}
								</div>
							</div>
						</a>
					</div>
				{% endfor %}
				{# fin de boucle for #}
			</section>
		</div>
	</div>


{% endblock %}
