<nav class="bg-white border-b border-gray-200 dark:bg-gray-900 dark:border-gray-700">
	<div class="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8">
		<div class="flex justify-between items-center h-16">
			<a href="{{ path('app_accueil') }}" class="text-2xl font-bold text-gray-900 dark:text-white">
				MAINBOOK
			</a>
			<div class="hidden md:flex space-x-8 rtl:space-x-reverse">
				<a href="{{ path('app_accueil') }}" class="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 {{ app.request.get('_route') == 'app_accueil' ? 'font-semibold text-blue-600 dark:text-blue-400' : '' }}">
					Accueil
				</a>
				<a href="{{ path('app_product') }}" class="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 {{ app.request.get('_route') in ['app_product', 'product_detail'] ? 'font-semibold text-blue-600 dark:text-blue-400' : '' }}">
					Les livres
				</a>
				{% if app.user %}
					<a href="{{ path('app_profil') }}" class="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 {{ app.request.get('_route') == 'app_profil' ? 'font-semibold text-blue-600 dark:text-blue-400' : '' }}">
						Mon compte
					</a>
					{# {% if 'ROLE_ADMIN' in app.user.roles %}
						<a href="{{ path('admin') }}" class="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 {{ app.request.get('_route') == 'admin' ? 'font-semibold text-blue-600 dark:text-blue-400' : '' }}">
							Admin
						</a>
					{% endif %} #}
				{% else %}
					<a href="{{ path('app_login') }}" class="text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 {{ app.request.get('_route') == 'app_login' ? 'font-semibold text-blue-600 dark:text-blue-400' : '' }}">
						Connexion
					</a>
				{% endif %}
			</div>
			<button data-collapse-toggle="navbar" type="button" class="md:hidden text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white focus:outline-none">
				<svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
					<path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"></path>
				</svg>
			</button>
		</div>
	</div>
	<div class="hidden md:hidden" id="navbar">
		<div class="px-4 pt-2 pb-3 space-y-1 sm:px-3">
			<a href="{{ path('app_accueil') }}" class="block text-gray-900 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 rounded py-2 px-3 {{ app.request.get('_route') == 'app_accueil' ? 'bg-blue-100 dark:bg-blue-800' : '' }}">
				Accueil
			</a>
			<a href="{{ path('app_product') }}" class="block text-gray-900 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 rounded py-2 px-3 {{ app.request.get('_route') in ['app_product', 'product_detail'] ? 'bg-blue-100 dark:bg-blue-800' : '' }}">
				Nos produits
			</a>
			{% if app.user %}
				<a href="{{ path('app_profil') }}" class="block text-gray-900 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 rounded py-2 px-3 {{ app.request.get('_route') == 'app_profil' ? 'bg-blue-100 dark:bg-blue-800' : '' }}">
					Mon compte
				</a>
				{% if 'ROLE_ADMIN' in app.user.roles %}
					<a href="{{ path('admin') }}" class="block text-gray-900 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 rounded py-2 px-3 {{ app.request.get('_route') == 'admin' ? 'bg-blue-100 dark:bg-blue-800' : '' }}">
						Admin
					</a>
				{% endif %}
			{% else %}
				<a href="{{ path('app_login') }}" class="block text-gray-900 dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700 rounded py-2 px-3 {{ app.request.get('_route') == 'app_login' ? 'bg-blue-100 dark:bg-blue-800' : '' }}">
					Connexion
				</a>
			{% endif %}
		</div>
	</div>
</nav>
<script>
	document.addEventListener('DOMContentLoaded', function () {
		const button = document.querySelector('[data-collapse-toggle="navbar"]');
		const navbar = document.getElementById('navbar');

		button.addEventListener('click', function () {
			navbar.classList.toggle('hidden');
		});
	});
</script>
